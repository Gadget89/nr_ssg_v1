import * as React from "react";

const Footer = () => {
  
    return (
      <footer className="container">
        <p style={{padding: "0 1rem 1rem"}}>&copy; {new Date().getFullYear()} Nic Roybal</p>
      </footer>
    );
};

export default Footer;
